import type { RuntimeMessage } from "allure-js-commons/sdk";
import { MessageTestRuntime } from "allure-js-commons/sdk/runtime";
export declare class MochaTestRuntime extends MessageTestRuntime {
    private readonly messageProcessor;
    constructor(messageProcessor: (...messages: RuntimeMessage[]) => void);
    sendMessage(message: RuntimeMessage): Promise<void>;
}
